#+TITLE: ASP Skill Contract

* ASP skill contract v1
:PROPERTIES:
:CONTRACT_ID: asp.skill.v1
:REFER_ORG: ./languages/org/templates/ASP_ORG_SKILL.org#asp-org
:CONTRACT_SCOPE: document
:CONTRACT_KIND: org-elements
:END:

** Contract binding keyword
:PROPERTIES:
:ASSERT_ID: asp.skill.contract.binding
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (keyword :descendant-of $scope :summary-contains (key "CONTRACT_ORG")))
#+END_SRC

** Contract metadata section
:PROPERTIES:
:ASSERT_ID: asp.skill.section.contract-metadata
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "Contract Metadata")))
#+END_SRC

** Runtime activation section
:PROPERTIES:
:ASSERT_ID: asp.skill.section.runtime-activation
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "Runtime Activation")))
#+END_SRC

** ASP Org reference section
:PROPERTIES:
:ASSERT_ID: asp.skill.section.asp-org-reference
:SEVERITY: error
:END:
#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "ASP Org Reference")))
#+END_SRC

** Runtime activation table
:PROPERTIES:
:ASSERT_ID: asp.skill.runtime-activation.table
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (table :descendant-of $scope))
#+END_SRC

** Agent operating contract section
:PROPERTIES:
:ASSERT_ID: asp.skill.section.agent-operating-contract
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "Agent Operating Contract")))
#+END_SRC

** Agent search contract sections
:PROPERTIES:
:ASSERT_ID: asp.skill.agent-operating.required-sections
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "Search Flow")))
#+END_SRC

** Dependency search topology section
:PROPERTIES:
:ASSERT_ID: asp.skill.agent-operating.dependency-search-topology
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "Dependency Search Topology")))
#+END_SRC

** Provider contracts section
:PROPERTIES:
:ASSERT_ID: asp.skill.section.provider-contracts
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "Provider Contracts")))
#+END_SRC

** Provider contract entries
:PROPERTIES:
:ASSERT_ID: asp.skill.provider-contract.entries
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (plain-text :descendant-of $scope :summary-contains (value "manifest, dependency edge, import owner")))
#+END_SRC

** Client surface section
:PROPERTIES:
:ASSERT_ID: asp.skill.section.client-surface
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (headline :descendant-of $scope :summary-contains (title "Client Surface")))
#+END_SRC

** Org capture explicit contract command
:PROPERTIES:
:ASSERT_ID: asp.skill.client-surface.org-capture-explicit-contract
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (plain-text :descendant-of $scope :summary-contains (value "asp org capture --contract CONTRACT_ID")))
#+END_SRC

** ASP Org skill source boundary
:PROPERTIES:
:ASSERT_ID: asp.skill.provider-contract.asp-org-source-boundary
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert exists
  (plain-text :descendant-of $scope :summary-contains (value "Do not edit languages/org/templates/ASP_ORG_SKILL.org directly")))
#+END_SRC
#+BEGIN_SRC jinja2 :name message
Top-level ASP skills must route ASP_ORG.org changes through contracts, templates, or materialization code instead of direct skill edits.
#+END_SRC
