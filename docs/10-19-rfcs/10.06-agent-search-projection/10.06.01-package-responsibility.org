# SPDX-FileCopyrightText: 2026 tao3k team and Contributors
#
# SPDX-License-Identifier: Apache-2.0 AND LGPL-2.1-or-later

* Agent Search Projection Package Responsibility
:PROPERTIES:
:PARENT_RFC: ../10.06-agent-search-projection.org
:STATUS: normative
:END:

** Projection ownership

Wire envelopes and byte payloads belong to =agent-semantic-client-protocol=.
They contain no text presentation, ranking, selector lookup, caching, or
lifecycle behavior.

Source-index, selector, projection, ranking, and resident-memory query behavior
belong to =agent-semantic-search=.  Human-readable source or callable-skeleton
presentation belongs to =agent-semantic-client=.  Explicit machine mode alone
renders the complete typed JSON frame.

Human Query presentation MUST dispatch on the receipt-bound V1 projection.
=source= bytes become a native-language Org Babel block.  A
=callable-skeleton= envelope MUST be admitted by the shared V1 schema and then
rendered through the compact Client-owned skeleton grammar in a =text= Babel
block whose =language= header preserves its provider language.  It MUST NOT be
printed as the complete semantic-projection JSON envelope or mislabeled as
native source.  Every materialization projection MUST equal the enclosing
receipt projection; mixed projection receipts fail closed.  Machine JSON mode
continues to preserve the complete typed frame.  This is a presentation-path
correction inside stable V1 and changes no schema or Runtime authority.

HTTP/gRPC admission and the server-owned query lifecycle belong to
=agent-semantic-runtime-server=.  The public =asp= binary belongs to the
=agent-semantic-client= package.  It may parse arguments, select a V1 client request,
connect to the resident Server, and render the typed terminal returned through
=agent-semantic-client=.  It may not implement search, query, provider
admission, generation selection, Runtime lifecycle, or artifact mutation.

There is no executable =agent-semantic-protocol= package.  Shared wire
contracts belong to =agent-semantic-client-protocol=; the public executable and
its command adapter belong to =agent-semantic-client=, whose package-local
command modules are not a wire-protocol authority.  Legacy build, install,
test, and documentation routes naming =agent-semantic-protocol --bin asp= are
removed rather than retained as aliases.

=agent-semantic-client-server= is only the stateless frame transport boundary.
It MUST NOT retain a client-session map, catalog authority, or generation state.

** Cross-language qualification

Scenario and Live Corpus qualification MUST enumerate the registered language
profiles from the shared Schema Manager registry.  Hard-coded Rust/Python/
Gerbil subsets are invalid.  Every registered search/query method traverses
the same ASP Client wire request, ASP Server admission, resident search-data owner,
and ASP Client presentation contract.
