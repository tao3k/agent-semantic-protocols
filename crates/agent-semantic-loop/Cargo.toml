[package]
name = "agent-semantic-loop"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Reusable interactive loop and choice-pane primitives for ASP agents."

[dependencies]
serde = { version = "1", features = ["derive"] }
