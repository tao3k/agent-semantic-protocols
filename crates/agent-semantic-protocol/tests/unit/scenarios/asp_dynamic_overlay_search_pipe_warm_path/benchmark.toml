harness = "libtest"
test = "asp_dynamic_overlay_search_pipe_warm_path_stays_inside_scenario_gate"
target_total = "5ms"
max_total = "25ms"
observed_total = "3ms"
regression_budget = "10ms"
memory_budget_bytes = 8388608
observed_memory_bytes = 0
route_source = "dynamic-overlay"
max_provider_process_count = 0
max_stdout_bytes = 8192
fallback_reason = "none"
target_rationale = "Explicit --source finder search pipe should use the client-owned dynamic overlay/root candidate path without provider startup, native finder process startup, or source-index lookup. The gate measures the rendered sourceTrace elapsedMs from the real CLI output so debug binary startup is not counted as search latency."

[observed_timings]
dynamic_overlay_collect = "3ms"
provider_process_count = "0us"
native_finder_process_count = "0us"
render = "0us"
