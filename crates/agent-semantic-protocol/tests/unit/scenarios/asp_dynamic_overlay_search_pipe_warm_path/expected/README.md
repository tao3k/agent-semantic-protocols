# Expected

The command returns compact search-pipe seeds from the dynamic overlay/root-walk path, skips provider process startup, skips native finder process startup, skips source-index, and renders an exact `query --selector` next command.
