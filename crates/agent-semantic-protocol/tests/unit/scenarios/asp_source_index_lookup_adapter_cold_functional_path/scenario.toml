id = "asp-source-index-lookup-adapter-cold-functional-path"
title = "ASP search crate resolves SourceIndex lookup through DB adapter on a cold fixture"
policy_ids = [
  "RUST-AGENT-ASP-FUNCTIONAL-SUBCOMMAND-SOURCE-INDEX-LOOKUP-COLD-001",
  "RUST-AGENT-ASP-PERF-SUBCOMMAND-SOURCE-INDEX-001",
]
agent_goal = "Keep the DB-backed source-index lookup adapter owned by agent-semantic-search: after a cold fixture refresh, the adapter must resolve candidates through the State Core client DB, avoid provider/native finder work during lookup, and keep line ranges out of executable identity."
reference_repositories = ["tao3k/agent-semantic-protocols"]
reference_patterns = [
  "agent_semantic_search::lookup_source_index_in_client_cache_dir",
  "sourceIndexHit=true",
  "providerProcessCount=0",
]
inputs = "inputs"
expected = "expected"
