harness = "libtest"
test = "asp_source_index_lookup_adapter_cold_functional_path_stays_inside_scenario_gate"
snapshot = "asp_source_index_lookup_adapter_cold_functional_path_stays_inside_scenario_gate"
target_total = "3ms"
max_total = "25ms"
observed_total = "3ms"
regression_budget = "10ms"
memory_budget_bytes = 8388608
observed_memory_bytes = 4194304
route_source = "source-index"
max_provider_process_count = 0
max_stdout_bytes = 8192
fallback_reason = "none"
target_rationale = "Cold functional SourceIndex lookup adapter gate validates the new agent-semantic-search owner boundary over a freshly refreshed DB fixture. The gate measures adapter lookup duration directly and does not count debug binary startup or source-index refresh setup as search latency."

[observed_timings]
source_index_lookup = "3ms"
provider_process_count = "0us"
native_finder_process_count = "0us"
