harness = "libtest"
test = "asp_source_index_search_pipe_warm_path_stays_inside_scenario_gate"
snapshot = "asp_source_index_search_pipe_warm_path_stays_inside_scenario_gate"
target_total = "3ms"
max_total = "25ms"
observed_total = "3ms"
regression_budget = "10ms"
memory_budget_bytes = 8388608
observed_memory_bytes = 4194304
route_source = "source-index"
max_provider_process_count = 0
max_stdout_bytes = 8192
fallback_reason = "none"
target_rationale = "Warm search pipe must use the Rust SQL SourceIndex as the candidate source and skip provider/native finder processes. The gate measures Rust SourceIndex lookup duration directly and validates the real CLI output shape separately so debug binary startup is not counted as search latency."

[observed_timings]
source_index_lookup = "3ms"
provider_process_count = "0ms"
native_finder_process_count = "0ms"
