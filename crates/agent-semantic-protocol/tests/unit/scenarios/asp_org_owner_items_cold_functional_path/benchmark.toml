harness = "libtest"
test = "asp_org_owner_items_cold_functional_path_stays_inside_scenario_gate"
target_total = "250ms"
max_total = "500ms"
observed_total = "1ms"
regression_budget = "100ms"
memory_budget_bytes = 67108864
observed_memory_bytes = 0
route_source = "dynamic-owner-items"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"
target_rationale = "Cold owner-items search is the first functional gate for Org. It exercises the embedded orgize provider surface through the manifest-compatible search owner-items route and verifies no retired query-selector owner route is required."

[observed_timings]
owner_items_cold_functional_path = "0us"
