# Julia owner-items cold functional path fixture

The scenario creates `src/Model.jl`, activates a fake `asp-julia-harness`, and
verifies the first request executes the provider-owned owner-items interface.
