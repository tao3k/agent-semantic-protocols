harness = "libtest"
test = "scenario_performance_gate::scenario_benchmark::asp_rust_owner_items_minimal_ast_cut_cold_functional_path_stays_inside_scenario_gate"
phase = "cold"
target_total = "10ms"
max_total = "25ms"
observed_total = "0ms"
regression_budget = "15ms"
memory_budget_bytes = 67108864
observed_memory_bytes = 0
target_rationale = "The explicit owner path is already known, so a minimal parser-owned AST projection must avoid source-index, FTS, finder, and provider startup work."
route_source = "dynamic-owner-items"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"

[observed_timings]
native_parse = "0ms"
ast_projection = "0ms"
