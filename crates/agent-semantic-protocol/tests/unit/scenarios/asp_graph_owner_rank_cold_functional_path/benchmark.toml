harness = "libtest"
test = "asp_graph_owner_rank_cold_functional_path_stays_inside_scenario_gate"
target_total = "500us"
max_total = "15ms"
observed_total = "500us"
regression_budget = "1ms"
memory_budget_bytes = 1048576
observed_memory_bytes = 0
target_rationale = "Owner ranking is an in-memory search crate policy over bounded graph candidates and should stay sub-millisecond on cold functional checks."
route_source = "graph-owner-rank"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"

[observed_timings]
rank = "500us"
provider_process_count = "0us"
native_finder_process_count = "0us"
