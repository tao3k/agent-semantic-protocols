harness = "libtest"
test = "asp_gerbil_deps_active_gxi_stdlib_hot_path_stays_inside_scenario_gate"
phase = "warm"
target_total = "25ms"
max_total = "300ms"
observed_total = "1ms"
regression_budget = "25ms"
memory_budget_bytes = 67108864
observed_memory_bytes = 1048576
route_source = "gerbil-deps-index"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"
target_rationale = "Gerbil standard-library deps search is a Rust DB index warm path. The scenario warms the asp binary once, then verifies the active-gxi stdlib query does not spawn gslph, does not scan a workspace, and stays inside a millisecond-scale hot-path budget."

[observed_timings]
gerbil_deps_search_hot_path = "1ms"
provider_process_count = "0us"
workspace_scan = "0us"
fallback = "0us"
