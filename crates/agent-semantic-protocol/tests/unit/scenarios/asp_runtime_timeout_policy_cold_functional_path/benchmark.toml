harness = "libtest"
test = "asp_runtime_timeout_policy_cold_functional_path_stays_inside_scenario_gate"
snapshot = "asp_runtime_timeout_policy_cold_functional_path_stays_inside_scenario_gate"
target_total = "500us"
max_total = "2ms"
observed_total = "500us"
regression_budget = "2ms"
memory_budget_bytes = 1048576
observed_memory_bytes = 262144
route_source = "runtime-timeout-policy"
max_provider_process_count = 0
max_stdout_bytes = 1024
fallback_reason = "none"
target_rationale = "Cold functional runtime timeout policy gate validates receipt construction only. It does not count command startup or provider process execution."

[observed_timings]
timeout_policy = "500us"
provider_process_count = "0us"
native_finder_process_count = "0us"
