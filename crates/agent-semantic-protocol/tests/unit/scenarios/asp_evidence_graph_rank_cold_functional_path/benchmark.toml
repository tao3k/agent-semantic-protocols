harness = "libtest"
test = "asp_evidence_graph_rank_cold_functional_path_stays_inside_scenario_gate"
snapshot = "asp_evidence_graph_rank_cold_functional_path_stays_inside_scenario_gate"
target_total = "1ms"
max_total = "50ms"
observed_total = "23ms"
regression_budget = "25ms"
memory_budget_bytes = 8388608
observed_memory_bytes = 1048576
route_source = "evidence-graph-rank"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"
target_rationale = "EvidenceGraph ranking is a Rust graph-route seed policy and must stay millisecond-scale without provider, native finder, DB, or command-layer work. The scenario measures only the bounded in-memory rank path over a cold fixture. max_total allows macOS CI cold scheduling variance while preserving a sub-100ms hard gate."

[observed_timings]
evidence_graph_rank = "23ms"
provider_process_count = "0us"
native_finder_process_count = "0us"
