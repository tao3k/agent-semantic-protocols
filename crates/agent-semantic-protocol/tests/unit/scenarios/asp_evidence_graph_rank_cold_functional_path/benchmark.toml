harness = "libtest"
test = "asp_evidence_graph_rank_cold_functional_path_stays_inside_scenario_gate"
snapshot = "asp_evidence_graph_rank_cold_functional_path_stays_inside_scenario_gate"
target_total = "1ms"
max_total = "5ms"
observed_total = "1ms"
regression_budget = "2ms"
memory_budget_bytes = 8388608
observed_memory_bytes = 1048576
route_source = "evidence-graph-rank"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"
target_rationale = "EvidenceGraph ranking is a Rust graph-route seed policy and must stay millisecond-scale without provider, native finder, DB, or command-layer work. The scenario measures only the ranking seam over a cold fixture."

[observed_timings]
evidence_graph_rank = "1ms"
provider_process_count = "0ms"
native_finder_process_count = "0ms"
