# TypeScript owner-items cold functional path fixture

The scenario creates `app/src/model.ts`, activates a fake `ts-harness`, and
verifies the first request executes the provider-owned owner-items interface.
