# Rust owner-items cold functional path fixture

The scenario creates `crate/src/lib.rs`, activates a fake `rs-harness`, and
verifies the first request executes the provider-owned owner-items interface.
