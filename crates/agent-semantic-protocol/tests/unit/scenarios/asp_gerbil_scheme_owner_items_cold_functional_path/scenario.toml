id = "asp-gerbil-scheme-projection-owner-items-lifecycle"
title = "ASP gerbil-scheme owner-items uses projection import then Turso graph reads"
policy_ids = ["GERBIL-SCHEME-AGENT-ASP-FUNCTIONAL-SUBCOMMAND-SEARCH-OWNER-COLD-001"]
agent_goal = "Prove cold owner-items returns an explicit projection action without provider startup, projection import invokes the harness once, and warm owner-items reads exact graph selectors without a second provider process."
reference_repositories = ["tao3k/agent-semantic-protocols"]
reference_patterns = [
  "asp gerbil-scheme projection import --owner src/model.ss --workspace .",
  "state=projection-cold-required providerProcessCount=0",
  "alg=graph-turbo-owner-items",
  "item:symbol(dynamic-owner-item-index)",
]
inputs = "inputs"
expected = "expected"
command = "asp gerbil-scheme search owner src/model.ss items --query dynamic-owner-item-index --workspace . --view seeds; asp gerbil-scheme projection import --owner src/model.ss --workspace .; asp gerbil-scheme search owner src/model.ss items --query dynamic-owner-item-index --workspace . --view seeds"
