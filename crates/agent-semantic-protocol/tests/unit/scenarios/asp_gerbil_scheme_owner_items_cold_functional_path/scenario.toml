id = "asp-gerbil-scheme-owner-items-cold-functional-path"
title = "ASP gerbil-scheme search owner items executes gslph owner-items on cold path"
policy_ids = ["GERBIL-SCHEME-AGENT-ASP-FUNCTIONAL-SUBCOMMAND-SEARCH-OWNER-COLD-001"]
agent_goal = "Prove the first asp gerbil-scheme search owner <path> items request executes the language harness owner-items interface and returns shared owner/item evidence before any hot cache optimization is considered."
reference_repositories = ["tao3k/agent-semantic-protocols"]
reference_patterns = [
  "asp gerbil-scheme search owner src/model.ss items --query dynamic-owner-item-index --workspace . --view seeds",
  "alg=gslph-owner-items",
  "item:symbol(dynamic-owner-item-index)",
]
inputs = "inputs"
expected = "expected"
command = "asp gerbil-scheme search owner src/model.ss items --query dynamic-owner-item-index --workspace . --view seeds"
