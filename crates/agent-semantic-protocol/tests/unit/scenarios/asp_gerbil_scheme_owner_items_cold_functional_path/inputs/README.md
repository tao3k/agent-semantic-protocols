# Gerbil Scheme owner-items cold functional path fixture

The scenario creates `src/model.ss`, activates a fake `gslph`, and verifies the
first request executes the provider-owned owner-items interface.
