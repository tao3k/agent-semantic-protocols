# Python owner-items cold functional path fixture

The scenario creates `src/model.py`, activates a fake `py-harness`, and verifies
the first request executes the provider-owned owner-items interface.
