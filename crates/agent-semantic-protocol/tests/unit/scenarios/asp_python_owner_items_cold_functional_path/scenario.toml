id = "asp-python-owner-items-cold-functional-path"
title = "ASP python search owner items executes py-harness owner-items on cold path"
policy_ids = ["PYTHON-AGENT-ASP-FUNCTIONAL-SUBCOMMAND-SEARCH-OWNER-COLD-001"]
agent_goal = "Prove the first asp python search owner <path> items request executes the language harness owner-items interface and returns shared owner/item evidence before any hot cache optimization is considered."
reference_repositories = ["tao3k/agent-semantic-protocols"]
reference_patterns = [
  "asp python search owner src/model.py items --query dynamic_owner_item_index --workspace . --view seeds",
  "alg=py-harness-owner-items",
  "item:symbol(dynamic_owner_item_index)",
]
inputs = "inputs"
expected = "expected"
command = "asp python search owner src/model.py items --query dynamic_owner_item_index --workspace . --view seeds"
