[package]
name = "agent-semantic-search-projection"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Typed language-neutral search projection model, resident result contract, and renderer for ASP"

[dependencies]
agent-semantic-content-identity = { path = "../agent-semantic-content-identity" }
agent-semantic-topology = { path = "../agent-semantic-topology" }
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
jsonschema.workspace = true

[build-dependencies]
asp-rust-project-harness-policy.workspace = true

[lints]
workspace = true
