harness = "libtest"
test = "asp_search_candidate_contract_cold_functional_path_stays_inside_scenario_gate"
snapshot = "asp_search_candidate_contract_cold_functional_path_stays_inside_scenario_gate"
target_total = "1ms"
max_total = "25ms"
observed_total = "15ms"
regression_budget = "10ms"
memory_budget_bytes = 4194304
observed_memory_bytes = 1048576
route_source = "search-candidate-contract"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"
target_rationale = "Cold functional candidate projection and merge gate validates the search-owned shared candidate contract. It measures only DTO projection, lexical overlay hit projection, and router-ready merge ordering, not command startup or provider execution. max_total allows macOS CI cold scheduling variance while keeping the gate in a small millisecond budget."

[observed_timings]
candidate_projection_and_merge = "15ms"
provider_process_count = "0us"
native_finder_process_count = "0us"
