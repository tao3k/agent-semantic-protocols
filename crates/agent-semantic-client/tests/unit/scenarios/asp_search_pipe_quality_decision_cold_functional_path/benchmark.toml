harness = "libtest"
test = "asp_search_pipe_quality_decision_cold_functional_path_stays_inside_scenario_gate"
target_total = "500us"
max_total = "10ms"
observed_total = "500us"
regression_budget = "1ms"
memory_budget_bytes = 1048576
observed_memory_bytes = 0
max_provider_process_count = 0
max_stdout_bytes = 4096
snapshot = "asp_search_pipe_quality_decision_cold_functional_path_stays_inside_scenario_gate"
fallback_reason = "none"
route_source = "search-pipe-quality-decision"
target_rationale = "Search-pipe quality decision is an in-memory search crate policy over bounded terms and candidate text. The target remains sub-millisecond; the low-millisecond hard ceiling absorbs parallel libtest scheduling jitter."

[observed_timings]
search_pipe_quality_decision = "500us"
provider_process_count = "0us"
native_finder_process_count = "0us"
