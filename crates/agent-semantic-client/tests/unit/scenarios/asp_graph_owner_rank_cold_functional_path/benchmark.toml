harness = "libtest"
test = "asp_graph_owner_rank_cold_functional_path_stays_inside_scenario_gate"
target_total = "500us"
max_total = "25ms"
observed_total = "17ms"
regression_budget = "8ms"
memory_budget_bytes = 1048576
observed_memory_bytes = 0
target_rationale = "Owner ranking is an in-memory search crate policy over bounded graph candidates and should stay sub-millisecond on cold functional checks. max_total allows macOS CI cold scheduling variance while keeping the gate in a small millisecond budget."
route_source = "graph-owner-rank"
max_provider_process_count = 0
max_stdout_bytes = 4096
fallback_reason = "none"

[observed_timings]
rank = "17ms"
provider_process_count = "0us"
native_finder_process_count = "0us"
