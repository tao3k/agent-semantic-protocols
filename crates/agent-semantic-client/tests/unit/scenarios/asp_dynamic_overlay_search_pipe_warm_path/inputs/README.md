# Input

Temporary Rust fixture with `src/lib.rs` containing `dynamic_overlay_fixture`.
The scenario runs:

```sh
asp rust search pipe dynamic_overlay_fixture --source finder --workspace . --view seeds
```
