# ASP rust owner-items cache hot path expected output
The hot command must render cached asp-rust owner-items output, keep provider and native finder process counts at zero for the measured run, and avoid executable line-range selectors.
