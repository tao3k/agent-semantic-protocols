# ASP rust owner-items cache hot path expected output
The hot command must render cached rust-harness owner-items output, keep provider and native finder process counts at zero for the measured run, and avoid executable line-range selectors.
