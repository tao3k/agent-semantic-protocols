This scenario models an already-known exact selector requested with
`--view metadata`.

Metadata is a direct inventory map. It can name headings, items, and next
selectors, but it is not final code or document evidence.
