# Expected

`SearchPipeSourceMode::Auto` for Org and Markdown document search uses the
search package lexical overlay route before parser document-element acquisition.
The provider/parser route remains explicit through `SearchPipeSourceMode::Provider`.
