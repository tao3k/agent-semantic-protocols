# Expected

Every `agent-semantic-search` unit and integration surface is represented in the
package-local scenario benchmark monitor. Adding a new search surface requires
updating this scenario and its `benchmark.toml` observed timings.
