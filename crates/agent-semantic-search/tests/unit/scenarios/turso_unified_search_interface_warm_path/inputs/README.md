# Turso Unified Search Inputs

The test creates a temporary State Core home, inserts stable search, dynamic overlay, and structural-index rows into the Turso-backed DB engine, then queries them through `agent-semantic-search`.
