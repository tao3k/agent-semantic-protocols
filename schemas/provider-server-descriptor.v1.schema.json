{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agent-semantic-protocols.dev/provider-server-descriptor.v1.schema.json",
  "title": "Provider Server Descriptor v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaId", "schemaVersion", "transport", "command", "healthPath", "requestPath", "shutdownPath", "warmupPolicy"],
  "properties": {
    "schemaId": { "const": "agent.semantic-protocols.provider-server-descriptor" },
    "schemaVersion": { "const": "1" },
    "transport": { "const": "http-json-v1" },
    "command": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
    "healthPath": { "type": "string", "pattern": "^/" },
    "requestPath": { "type": "string", "pattern": "^/" },
    "shutdownPath": { "type": "string", "pattern": "^/" },
    "warmupPolicy": { "enum": ["before-ready"] }
  }
}
