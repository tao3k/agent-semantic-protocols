#!/bin/sh
set -eu

asp_hook_state_home=${ASP_STATE_HOME:-${HOME}/.agent-semantic-protocols}

if ! exec "$asp_hook_state_home/hooks/bin/asp-hook-evaluator" "$@"; then
    printf '%s\n' '{"systemMessage":"ASP Hook evaluator is unavailable."}'
    exit 0
fi
