SPECIFICATION Spec
CHECK_DEADLOCK FALSE

CONSTANTS
  MaxEvidence = 30
  QueryGrammarText = "asp query playbook '(query (producers (language <producer>)) (select (selectors \"<exact-selector>\") (projection <callable-skeleton|source>)))'"

INVARIANTS
  TypeOK
  TopKBound
  QueryGrammarExactlyWhenEvidenceExists
  SearchNeverMaterializesSource
  SourceOnlyAfterAgentQuery
  ProviderDoesNotChooseContinuation
  HandoffKeepsAgentChoicesOpen
